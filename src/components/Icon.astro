---
const { className="", name, fill=false } = Astro.props
---

<svg class={`icon ${className}`} fill={fill ? "currentColor" : "none"}>
  <use xlink:href={`/src/styles/feather-sprite.svg#${name}`} />
</svg>

<style>
.icon {
  width: 1rem;
  height: 1rem;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}
</style>
